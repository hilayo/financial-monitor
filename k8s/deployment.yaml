apiVersion: apps/v1
kind: Deployment
metadata:
  name: financial-monitor
  labels:
    app: financial-monitor
spec:
  replicas: 3
  selector:
    matchLabels:
      app: financial-monitor
  template:
    metadata:
      labels:
        app: financial-monitor
    spec:
      containers:
        - name: api
          image: financial-monitor:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: financial-monitor-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          emptyDir: {}
